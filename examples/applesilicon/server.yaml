apiVersion: applesilicon.scaleway.upbound.io/v1alpha1
kind: Server
metadata:
  name: applesilicon-server
spec:
  description: "Managed with Crossplane Github Provider (generated with Upjet)"
  visibility: public
  forProvider:
    name: crossplane-applesilicon-server
    type: M1-M
  providerConfigRef:
    name: default
