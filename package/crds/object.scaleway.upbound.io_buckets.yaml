---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: buckets.object.scaleway.upbound.io
spec:
  group: object.scaleway.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - scaleway
    kind: Bucket
    listKind: BucketList
    plural: buckets
    singular: bucket
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Bucket is the Schema for the Buckets API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BucketSpec defines the desired state of Bucket
            properties:
              deletionPolicy:
                default: Delete
                description: 'DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource. This field is planned to be deprecated
                  in favor of the ManagementPolicies field in a future release. Currently,
                  both could be set independently and non-default values would be
                  honored if the feature flag is enabled. See the design doc for more
                  information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223'
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  acl:
                    description: '(Deprecated) The canned ACL you want to apply to
                      the bucket. ACL of the bucket: either ''public-read'' or ''private''.'
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  forceDestroy:
                    description: Enable deletion of objects in bucket before destroying,
                      locked objects or under legal hold are also deleted and not
                      recoverable Delete objects in bucket
                    type: boolean
                  lifecycleRule:
                    description: Lifecycle configuration is a set of rules that define
                      actions that Scaleway Object Storage applies to a group of objects
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                            Specifies the number of days after initiating a multipart
                            upload when the multipart upload must be completed
                          type: number
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Specifies if the
                            configuration rule is Enabled or Disabled
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below). Specifies a period in the object's expire
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                            type: object
                          type: array
                        id:
                          description: Unique identifier for the rule. Must be less
                            than or equal to 255 characters in length. Unique identifier
                            for the rule
                          type: string
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies. The prefix identifying one
                            or more objects to which the rule applies
                          type: string
                        tags:
                          additionalProperties:
                            type: string
                          description: A list of tags (key / value) for the bucket.
                            The tags associated with the bucket lifecycle
                          type: object
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below). Define when objects transition to
                            another storage class
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                              storageClass:
                                description: Specifies the Scaleway storage class
                                  STANDARD, GLACIER, ONEZONE_IA  to which you want
                                  the object to transition. Specifies the Scaleway
                                  Object Storage class to which you want the object
                                  to transition
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: The name of the bucket. The name of the bucket
                    type: string
                  objectLockEnabled:
                    description: The element value can be either Enabled or Disabled.
                      If a rule is disabled, Scaleway S3 doesn't perform any of the
                      actions defined in the rule. Enable object lock
                    type: boolean
                  projectId:
                    description: (Defaults to provider project_id) The ID of the project
                      the bucket is associated with. The project_id you want to attach
                      the resource to
                    type: string
                  region:
                    description: The region in which the bucket should be created.
                      The region you want to attach the resource to
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A list of tags (key / value) for the bucket. The
                      tags associated with this bucket
                    type: object
                  versioning:
                    description: A state of versioning (documented below) Allow multiple
                      versions of an object in the same bucket
                    items:
                      properties:
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Enable versioning.
                            Once you version-enable a bucket, it can never return
                            to an unversioned state
                          type: boolean
                      type: object
                    type: array
                type: object
              initProvider:
                description: THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. InitProvider
                  holds the same fields as ForProvider, with the exception of Identifier
                  and other resource reference fields. The fields that are in InitProvider
                  are merged into ForProvider when the resource is created. The same
                  fields are also added to the terraform ignore_changes hook, to avoid
                  updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after
                  creation, for example because of an external controller is managing
                  them, like an autoscaler.
                properties:
                  acl:
                    description: '(Deprecated) The canned ACL you want to apply to
                      the bucket. ACL of the bucket: either ''public-read'' or ''private''.'
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  forceDestroy:
                    description: Enable deletion of objects in bucket before destroying,
                      locked objects or under legal hold are also deleted and not
                      recoverable Delete objects in bucket
                    type: boolean
                  lifecycleRule:
                    description: Lifecycle configuration is a set of rules that define
                      actions that Scaleway Object Storage applies to a group of objects
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                            Specifies the number of days after initiating a multipart
                            upload when the multipart upload must be completed
                          type: number
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Specifies if the
                            configuration rule is Enabled or Disabled
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below). Specifies a period in the object's expire
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                            type: object
                          type: array
                        id:
                          description: Unique identifier for the rule. Must be less
                            than or equal to 255 characters in length. Unique identifier
                            for the rule
                          type: string
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies. The prefix identifying one
                            or more objects to which the rule applies
                          type: string
                        tags:
                          additionalProperties:
                            type: string
                          description: A list of tags (key / value) for the bucket.
                            The tags associated with the bucket lifecycle
                          type: object
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below). Define when objects transition to
                            another storage class
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                              storageClass:
                                description: Specifies the Scaleway storage class
                                  STANDARD, GLACIER, ONEZONE_IA  to which you want
                                  the object to transition. Specifies the Scaleway
                                  Object Storage class to which you want the object
                                  to transition
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: The name of the bucket. The name of the bucket
                    type: string
                  objectLockEnabled:
                    description: The element value can be either Enabled or Disabled.
                      If a rule is disabled, Scaleway S3 doesn't perform any of the
                      actions defined in the rule. Enable object lock
                    type: boolean
                  projectId:
                    description: (Defaults to provider project_id) The ID of the project
                      the bucket is associated with. The project_id you want to attach
                      the resource to
                    type: string
                  region:
                    description: The region in which the bucket should be created.
                      The region you want to attach the resource to
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A list of tags (key / value) for the bucket. The
                      tags associated with this bucket
                    type: object
                  versioning:
                    description: A state of versioning (documented below) Allow multiple
                      versions of an object in the same bucket
                    items:
                      properties:
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Enable versioning.
                            Once you version-enable a bucket, it can never return
                            to an unversioned state
                          type: boolean
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: 'THIS IS AN ALPHA FIELD. Do not use it in production.
                  It is not honored unless the relevant Crossplane feature flag is
                  enabled, and may be changed or removed without notice. ManagementPolicies
                  specify the array of actions Crossplane is allowed to take on the
                  managed and external resources. This field is planned to replace
                  the DeletionPolicy field in a future release. Currently, both could
                  be set independently and non-default values would be honored if
                  the feature flag is enabled. If both are custom, the DeletionPolicy
                  field will be ignored. See the design doc for more information:
                  https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md'
                items:
                  description: A ManagementAction represents an action that the Crossplane
                    controllers can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: BucketStatus defines the observed state of Bucket.
            properties:
              atProvider:
                properties:
                  acl:
                    description: '(Deprecated) The canned ACL you want to apply to
                      the bucket. ACL of the bucket: either ''public-read'' or ''private''.'
                    type: string
                  apiEndpoint:
                    description: The endpoint URL of the bucket API URL of the bucket
                    type: string
                  corsRule:
                    description: A rule of Cross-Origin Resource Sharing (documented
                      below).
                    items:
                      properties:
                        allowedHeaders:
                          description: Specifies which headers are allowed.
                          items:
                            type: string
                          type: array
                        allowedMethods:
                          description: Specifies which methods are allowed. Can be
                            GET, PUT, POST, DELETE or HEAD.
                          items:
                            type: string
                          type: array
                        allowedOrigins:
                          description: Specifies which origins are allowed.
                          items:
                            type: string
                          type: array
                        exposeHeaders:
                          description: Specifies expose header in the response.
                          items:
                            type: string
                          type: array
                        maxAgeSeconds:
                          description: Specifies time in seconds that browser can
                            cache the response for a preflight request.
                          type: number
                      type: object
                    type: array
                  endpoint:
                    description: The endpoint URL of the bucket Endpoint of the bucket
                    type: string
                  forceDestroy:
                    description: Enable deletion of objects in bucket before destroying,
                      locked objects or under legal hold are also deleted and not
                      recoverable Delete objects in bucket
                    type: boolean
                  id:
                    description: Unique identifier for the rule. Must be less than
                      or equal to 255 characters in length.
                    type: string
                  lifecycleRule:
                    description: Lifecycle configuration is a set of rules that define
                      actions that Scaleway Object Storage applies to a group of objects
                    items:
                      properties:
                        abortIncompleteMultipartUploadDays:
                          description: Specifies the number of days after initiating
                            a multipart upload when the multipart upload must be completed.
                            Specifies the number of days after initiating a multipart
                            upload when the multipart upload must be completed
                          type: number
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Specifies if the
                            configuration rule is Enabled or Disabled
                          type: boolean
                        expiration:
                          description: Specifies a period in the object's expire (documented
                            below). Specifies a period in the object's expire
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                            type: object
                          type: array
                        id:
                          description: Unique identifier for the rule. Must be less
                            than or equal to 255 characters in length. Unique identifier
                            for the rule
                          type: string
                        prefix:
                          description: Object key prefix identifying one or more objects
                            to which the rule applies. The prefix identifying one
                            or more objects to which the rule applies
                          type: string
                        tags:
                          additionalProperties:
                            type: string
                          description: A list of tags (key / value) for the bucket.
                            The tags associated with the bucket lifecycle
                          type: object
                        transition:
                          description: Specifies a period in the object's transitions
                            (documented below). Define when objects transition to
                            another storage class
                          items:
                            properties:
                              days:
                                description: Specifies the number of days after object
                                  creation when the specific rule action takes effect.
                                  Specifies the number of days after object creation
                                  when the specific rule action takes effect
                                type: number
                              storageClass:
                                description: Specifies the Scaleway storage class
                                  STANDARD, GLACIER, ONEZONE_IA  to which you want
                                  the object to transition. Specifies the Scaleway
                                  Object Storage class to which you want the object
                                  to transition
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  name:
                    description: The name of the bucket. The name of the bucket
                    type: string
                  objectLockEnabled:
                    description: The element value can be either Enabled or Disabled.
                      If a rule is disabled, Scaleway S3 doesn't perform any of the
                      actions defined in the rule. Enable object lock
                    type: boolean
                  projectId:
                    description: (Defaults to provider project_id) The ID of the project
                      the bucket is associated with. The project_id you want to attach
                      the resource to
                    type: string
                  region:
                    description: The region in which the bucket should be created.
                      The region you want to attach the resource to
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A list of tags (key / value) for the bucket. The
                      tags associated with this bucket
                    type: object
                  versioning:
                    description: A state of versioning (documented below) Allow multiple
                      versions of an object in the same bucket
                    items:
                      properties:
                        enabled:
                          description: The element value can be either Enabled or
                            Disabled. If a rule is disabled, Scaleway S3 doesn't perform
                            any of the actions defined in the rule. Enable versioning.
                            Once you version-enable a bucket, it can never return
                            to an unversioned state
                          type: boolean
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
